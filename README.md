# Twitch Ads Blocker (Easy Wrapper)

This project packages the `vaft` engine from [pixeltris/TwitchAdSolutions](https://github.com/pixeltris/TwitchAdSolutions) as a load-unpacked browser extension, so users do not need uBlock advanced config or a userscript manager.

## Why this exists

Most Twitch ad-block solutions work as scripts, but setup is difficult for non-technical users. This repo focuses on:

- simple install (load unpacked extension)
- simple updates (`./scripts/update-vaft.sh`)
- reproducible packaging (`./scripts/build-zip.sh`)

## Quick start

1. Update bundled engine:
   ```bash
   ./scripts/update-vaft.sh
   ```
2. Build zip (optional):
   ```bash
   ./scripts/build-zip.sh
   ```
3. Install the extension:
   - Chrome/Edge: open `chrome://extensions`, enable **Developer mode**, click **Load unpacked**, select `extension/`.
   - Firefox: open `about:debugging#/runtime/this-firefox`, click **Load Temporary Add-on**, select `extension/manifest.json`.
4. Use the extension popup:
   - Click the extension icon.
   - Toggle **Ad Blocking** on/off.
   - Click **Reload Twitch Tab** to apply changes immediately.
   - Check the conflict status line; if a conflict is detected, disable other Twitch-specific ad blockers and reload Twitch.

## Files

- `extension/manifest.json`: MV3 extension manifest
- `extension/inject.js`: content script that injects the ad-block engine into page context
- `extension/injected/conflict-detector.js`: page-context detector for likely ad-block conflicts
- `extension/popup.html`: extension popup UI
- `extension/popup.css`: popup styling
- `extension/popup.js`: popup logic (toggle, reload, version + conflict display)
- `extension/injected/vaft.js`: vendored `vaft` engine (generated by update script)
- `extension/injected/upstream.txt`: source/version/commit metadata from last sync
- `scripts/update-vaft.sh`: pulls latest upstream `vaft.user.js` and strips userscript metadata
- `scripts/build-zip.sh`: creates `dist/twitch-ads-blocker-extension.zip`

## Limitations

- Twitch ad delivery changes often, so breakage can happen without warning.
- Any ad-blocking method may intermittently buffer or freeze depending on current Twitch behavior.
- This project wraps upstream logic; it does not guarantee ad-free playback on every stream and browser version.

## Attribution

Core ad-block logic is from `pixeltris/TwitchAdSolutions` (`vaft`), licensed under MIT.

- upstream repo: <https://github.com/pixeltris/TwitchAdSolutions>
- license copy: `third_party/LICENSE-pixeltris-TwitchAdSolutions.txt`
